# University Aggregator - Frontend

Современный frontend для агрегатора университетов, построенный на Next.js с TypeScript и Tailwind CSS.

## 🚀 Технологический стек

- **Next.js 15** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Утилитарный CSS фреймворк
- **React 19** - Библиотека для UI

## 📁 Структура проекта

```
frontend/
├── app/                          # Next.js App Router
│   ├── page.tsx                 # Главная страница
│   ├── layout.tsx               # Корневой layout с навигацией
│   ├── universities/
│   │   ├── page.tsx             # Список университетов
│   │   └── [id]/page.tsx        # Страница отдельного университета
│   └── chat/
│       └── page.tsx             # AI-чат
├── src/
│   ├── components/
│   │   ├── ui/                  # Базовые UI компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Select.tsx
│   │   │   └── index.ts
│   │   ├── university/          # Компоненты университетов
│   │   │   ├── UniversityCard.tsx
│   │   │   ├── UniversityFilters.tsx
│   │   │   └── index.ts
│   │   └── chat/                # Компоненты чата
│   │       ├── ChatMessage.tsx
│   │       ├── ChatInput.tsx
│   │       ├── ChatWindow.tsx
│   │       └── index.ts
│   ├── services/
│   │   ├── api.ts               # API сервис для университетов
│   │   └── chat.ts              # API сервис для чата
│   ├── types/
│   │   ├── university.ts        # Типы для университетов
│   │   └── chat.ts              # Типы для чата
│   └── lib/
│       └── mockData.ts          # Mock данные для разработки
├── tailwind.config.ts           # Конфигурация Tailwind
├── tsconfig.json                # TypeScript конфигурация
└── package.json
```

## 🎨 Дизайн

- **Современный минимализм** с tech-стилем
- **Нейтральная палитра** (серый, белый) с **синими акцентами**
- **Glassmorphism** эффекты и soft shadows
- **Адаптивный дизайн** для всех устройств
- **Плавные анимации** при hover

## 🔧 Установка и запуск

### Требования
- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
cd frontend
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Сборка для production

```bash
npm run build
npm start
```

## 📝 Переменные окружения

Создай файл `.env.local` на основе `.env.example`:

```bash
cp .env.example .env.local
```

Отредактируй переменные:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

## 🔌 Интеграция с Backend

### API Endpoints (TODO)

Все API вызовы находятся в `src/services/`:

- **`api.ts`** - Сервис для работы с университетами
  - `getUniversities()` - Получить список университетов
  - `getUniversity(id)` - Получить информацию об университете
  - `getCountries()` - Получить список стран
  - `getSpecialties()` - Получить список специальностей

- **`chat.ts`** - Сервис для работы с AI-чатом
  - `sendMessage()` - Отправить сообщение
  - `getChatHistory()` - Получить историю чата
  - `createSession()` - Создать новую сессию

### Подключение Backend

1. Обнови `NEXT_PUBLIC_API_URL` в `.env.local`
2. Раскомментируй API вызовы в компонентах (они помечены `TODO`)
3. Удали mock данные из `mockData.ts`

## 📄 Страницы

### Главная (`/`)
- Героический баннер с CTA
- Описание возможностей платформы
- Ссылки на основные разделы

### Университеты (`/universities`)
- Список всех университетов в виде карточек
- Фильтрация по стране, специальности, поиск
- Адаптивная сетка (2 колонны на desktop, 1 на mobile)

### Страница университета (`/universities/[id]`)
- Полная информация об университете
- Требования для поступления
- Список специальностей
- CTA для обращения к AI-ассистенту

### AI-Чат (`/chat`)
- Интерфейс чата с историей сообщений
- Ввод сообщений с поддержкой Enter
- Mock AI ответы (готово к интеграции с backend)

## 🎯 Компоненты

### UI Компоненты

- **Button** - Кнопка с вариантами (primary, secondary, outline)
- **Card** - Карточка с опциональным hover эффектом
- **Input** - Текстовое поле с label и error
- **Select** - Выпадающий список

### Специализированные компоненты

- **UniversityCard** - Карточка университета
- **UniversityFilters** - Панель фильтрации
- **ChatMessage** - Сообщение в чате
- **ChatInput** - Ввод сообщения
- **ChatWindow** - Окно чата с историей

## 🚀 Готовые к интеграции места

Все места для интеграции с backend помечены комментариями `TODO`:

1. **`src/services/api.ts`** - Замени mock URL на реальный backend
2. **`src/services/chat.ts`** - Подключи реальный AI сервис
3. **`app/universities/page.tsx`** - Раскомментируй API вызовы
4. **`app/universities/[id]/page.tsx`** - Раскомментируй API вызовы
5. **`app/chat/page.tsx`** - Раскомментируй API вызовы
6. **`src/components/university/UniversityFilters.tsx`** - Раскомментируй загрузку фильтров

## 📦 Production Deployment

### Vercel (рекомендуется)

```bash
npm install -g vercel
vercel
```

### Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 🔒 Security

- Все API вызовы используют HTTPS в production
- Переменные окружения не попадают в клиентский код (кроме `NEXT_PUBLIC_*`)
- Входные данные валидируются на клиенте и должны валидироваться на backend

## 📱 Адаптивность

Проект полностью адаптивен:
- **Mobile** - 1 колонна, оптимизированные размеры
- **Tablet** - 2 колонны
- **Desktop** - 3+ колонны, полная функциональность

## 🎓 Лучшие практики

- ✅ TypeScript для типизации
- ✅ Компонентная архитектура
- ✅ Разделение concerns (UI, services, types)
- ✅ Переиспользуемые компоненты
- ✅ Чистый, читаемый код
- ✅ Production-ready структура

## 📞 Поддержка

Для вопросов и предложений обращайся к команде разработки.
