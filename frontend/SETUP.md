# 🚀 Быстрый старт

## Установка

```bash
cd frontend
npm install
```

## Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на **http://localhost:3000**

## Сборка для production

```bash
npm run build
npm start
```

## 📋 Что уже готово

✅ **Главная страница** - Героический баннер с описанием возможностей  
✅ **Список университетов** - 6 mock университетов с карточками  
✅ **Фильтрация** - По стране, специальности, поиск  
✅ **Страница университета** - Полная информация, требования, специальности  
✅ **AI-чат** - Интерфейс с mock ответами  
✅ **Навигация** - Sticky header с меню  
✅ **Footer** - С ссылками и контактами  
✅ **Адаптивный дизайн** - Mobile, tablet, desktop  
✅ **TypeScript** - Полная типизация  
✅ **Tailwind CSS** - Современный стиль  

## 🔌 Интеграция с Backend

Все места для интеграции помечены `TODO` комментариями:

### 1. Обновить API URL
**Файл:** `frontend/.env.local`
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

### 2. Раскомментировать API вызовы

**Файл:** `services/api.ts`
- Раскомментируй реальные API вызовы вместо mock

**Файл:** `services/chat.ts`
- Раскомментируй вызовы к AI сервису

**Файл:** `app/universities/page.tsx`
- Раскомментируй `universityService.getUniversities()`

**Файл:** `app/universities/[id]/page.tsx`
- Раскомментируй `universityService.getUniversity(id)`

**Файл:** `app/chat/page.tsx`
- Раскомментируй `chatService.createSession()`
- Раскомментируй `chatService.sendMessage()`

**Файл:** `components/university/UniversityFilters.tsx`
- Раскомментируй загрузку фильтров из API

### 3. Удалить mock данные
**Файл:** `lib/mockData.ts` - можно удалить после подключения backend

## 📁 Структура проекта

```
frontend/
├── app/                          # Next.js App Router
│   ├── page.tsx                 # Главная
│   ├── layout.tsx               # Layout с навигацией
│   ├── universities/
│   │   ├── page.tsx             # Список
│   │   └── [id]/page.tsx        # Детали
│   └── chat/
│       └── page.tsx             # AI-чат
├── components/
│   ├── ui/                      # Button, Card, Input, Select
│   ├── university/              # UniversityCard, UniversityFilters
│   └── chat/                    # ChatMessage, ChatInput, ChatWindow
├── services/
│   ├── api.ts                   # API для университетов
│   └── chat.ts                  # API для чата
├── types/
│   ├── university.ts            # Типы университетов
│   └── chat.ts                  # Типы чата
├── lib/
│   └── mockData.ts              # Mock данные
└── tailwind.config.ts           # Tailwind конфиг
```

## 🎨 Дизайн

- **Цвета:** Синий (#3B82F6) + нейтральная палитра
- **Компоненты:** Карточки, кнопки, фильтры
- **Эффекты:** Hover анимации, soft shadows
- **Адаптивность:** Mobile-first подход

## 🧪 Тестирование

### Главная страница
- http://localhost:3000

### Список университетов
- http://localhost:3000/universities
- Попробуй фильтры (страна, специальность)

### Страница университета
- http://localhost:3000/universities/1

### AI-чат
- http://localhost:3000/chat
- Напиши сообщение и отправь

## 📝 Переменные окружения

Создай `.env.local`:
```bash
cp .env.example .env.local
```

Отредактируй:
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

## 🚨 Важно

- Все API вызовы готовы к интеграции (помечены `TODO`)
- Mock данные используются для разработки
- TypeScript обеспечивает типизацию
- Компоненты переиспользуемые и модульные

## 📚 Дополнительно

- Полная документация в `README.md`
- Все компоненты имеют JSDoc комментарии
- Код следует best practices React + Next.js
- Production-ready структура

## 🎯 Следующие шаги

1. Подключи backend API
2. Замени mock данные на реальные
3. Добавь аутентификацию (если нужна)
4. Настрой CI/CD
5. Задеплой на Vercel или другой хостинг

---

**Готово к разработке!** 🚀
